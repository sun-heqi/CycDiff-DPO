dataset:
  test:
    class: PromptDataset
    mmap_dir: ./datasets/LNR_CPSea/processed
    backbone_only: false
    use_covariance_matrix: true
    text_guidance: The distance between the N-terminal and C-terminal atoms of the peptide is less than 3.8 Å.
    
dataloader:
  num_workers: 4
  batch_size: 96

backbone_only: false
n_samples: 5
guidance_strength: 5
CADS_sampler: false

# sample_opt:
#   energy_func: condition2
#   energy_lambda: 50
